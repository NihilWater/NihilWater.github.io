# loss 函数

## 二次损失函数

## sigmoid_focal_loss_jit
```python
sigmoid_focal_loss_jit(
            pred,
            class_target,
            alpha=,
            gamma=,
            reduction="sum"
        )
```

